echo "Affirmative!"

if [ -f /tmp/radio-paradise ]
then
  killall mplayer
  rm /tmp/radio-paradise
else
  killall mplayer
  mplayer -slave -playlist /home/azer/.config/rp.m3u>/tmp/radio-paradise &
fi
echo "Bye!"
